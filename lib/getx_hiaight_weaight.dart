import 'package:flutter/material.dart';
class FirstPage2 extends StatelessWidget {
  // var name;

  FirstPage2({super.key,});

  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Scaffold(
        appBar: AppBar(),

      ),
    );
  }
}
